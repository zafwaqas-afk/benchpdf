"""Single source of truth for the app's identity and version."""

APP_NAME = "BenchPDF"
VERSION = "1.0.0"
PUBLISHER = "BenchPDF"

# Where the source is offered, to satisfy the AGPL's source-availability
# requirement (PyMuPDF is AGPL, so the whole app is distributed under AGPL-3.0).
SOURCE_URL = "https://example.invalid/benchpdf-source"  # replace with your real source location
