# Runtime dependencies for the PDF -> PowerPoint tool.
PyMuPDF==1.28.0
python-pptx==1.0.2
Flask==3.1.3
pywin32==312

# Image conversions: image -> PDF (img2pdf) and HEIC/HEIF decode (pillow_heif).
img2pdf==0.6.3
pillow_heif==1.5.0

# System-tray icon for the local app.
pystray==0.19.5

# Used only to generate the bundled test PDFs (not needed at runtime):
reportlab==5.0.0
